'use strict';

describe('votes', function() {
  describe('get', function() {
    it('should succeed to get three random movie list for vote');
  });

  describe('put', function() {
    it('should succeed to vote');
    it('should fail to vote if not exist movie id');
    it('should fail to vote if not exist user id');
  });
});
